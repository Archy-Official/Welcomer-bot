export default function handleGuildCreate(guild, guildConfigCache) {
  console.log(`[Bot Action Alert] Gateway Bot successfully introduced to a new client instance: ${guild.name} (ID: ${guild.id})`);
}
